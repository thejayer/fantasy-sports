{
  "name": "strictly-jayers-web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --turbopack",
    "build": "next build",
    "start": "next start",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:e2e": "playwright test e2e/smoke.spec.ts",
    "test:e2e:auth": "AUTH_DEV_BYPASS=0 PORT=3001 PLAYWRIGHT_BASE_URL=http://127.0.0.1:3001 playwright test e2e/auth-redirect.spec.ts",
    "verify:prerender": "node scripts/assert-no-stale-prerender.mjs",
    "verify:bundle-budget": "node scripts/check-bundle-budget.mjs"
  },
  "dependencies": {
    "next": "^16.2.12",
    "next-auth": "^5.0.0-beta.32",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "@testing-library/jest-dom": "^7.0.0",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "@types/node": "^22",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "^16.2.12",
    "jsdom": "^29.1.1",
    "typescript": "^5",
    "vitest": "^4.1.10"
  },
  "_overrides_note": [
    "Revisited under Next 16.2 (roadmap 1.7): next still pins postcss 8.4.31",
    "and sharp ^0.34.5; npm advisory for brace-expansion only clears at 5.0.8,",
    "which breaks minimatch@3 — so minimatch is overridden to ^10.2.6 as well.",
    "postcss: next pins 8.4.31; 8.5.x fixes XSS + sourceMappingURL path traversal.",
    "sharp: next allows ^0.34.5; 0.35.x picks up the patched libvips."
  ],
  "overrides": {
    "brace-expansion": "^5.0.8",
    "minimatch": "^10.2.6",
    "postcss": "^8.5.23",
    "sharp": "^0.35.3"
  }
}
