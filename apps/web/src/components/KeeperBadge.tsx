/**
 * Marks a kept player on roster / draft boards (roadmap 7.9b).
 * Visible label — colour alone is not the signal.
 */
export function KeeperBadge() {
  return (
    <span className="keeper-badge" title="Kept from a prior season">
      Keeper
    </span>
  );
}
